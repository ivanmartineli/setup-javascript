const Films = require("../models/author");


// Display detail page for a specific BookInstance.
exports.getFilms = (req, res) => {
    res.send(`NOT IMPLEMENTED: BookInstance detail: ${req.params.id}`);
  };